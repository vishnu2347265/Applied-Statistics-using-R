# Create a new factor variable for gender
data$gender <- as.factor(data$X2..Gender)
# Exclude missing values
data_no_missing <- na.omit(data)
# Perform one-way ANOVA
anova_result <- aov(exercise_frequency ~ gender, data = data_no_missing)
# Display the results
cat("One-Way ANOVA Results:\n")
## One-Way ANOVA Results:
print(summary(anova_result))
##              Df Sum Sq Mean Sq F value Pr(>F)
## gender       2    3.5   1.748   0.535  0.586
## Residuals  155  505.8   3.264
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one gender group has a different average frequency of engaging in physical exercise per week.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average frequency of engaging in physical exercise per week across all gender groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
# Mapping the response options to numerical values
data$exercise_frequency <- factor(data$X4..How.often.do.you.engage.in.physical.exercise.per.week.,
levels = c("Never", "1 - 2 times", "3 - 4 times", "5 or more times"),
labels = c(0, 1, 3.5, 5))  # Assigning numerical values based on the midpoints
# Convert the factor variable to numeric
data$exercise_frequency <- as.numeric(as.character(data$exercise_frequency))
# Create a new factor variable for age groups
data$age_group <- cut(data$X1..Age, breaks = c(15, 25, 30, 35), labels = c("15-25", "26-30", "31-35"))
# Exclude missing values
data_no_missing <- na.omit(data)
# Perform one-way ANOVA
anova_result <- aov(exercise_frequency ~ age_group, data = data_no_missing)
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Mapping the response options to numerical values
data$exercise_duration <- factor(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...,
levels = c("Less than 15 minutes", "15-30 minutes", "31-45 minutes", "46-60 minutes", "More than 60 minutes"),
labels = c(7.5, 22.5, 38, 53.5, 75))  # Assigning numerical values based on the midpoints
# Convert the factor variable to numeric
data$exercise_duration <- as.numeric(as.character(data$exercise_duration))
# Create a new factor variable for age groups
data$age_group <- cut(data$X1..Age, breaks = c(15, 25, 30, 35), labels = c("15-25", "26-30", "31-35"))
# Exclude missing values
data_no_missing <- na.omit(data)
# Perform one-way ANOVA
anova_result <- aov(exercise_duration ~ age_group, data = data_no_missing)
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one age group has a different average exercise duration.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in average exercise duration across all age groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
unique_values <- unique(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...)
print(unique_values)
# Exclude non-numeric values from the exercise duration variable
data$exercise_duration <- as.numeric(as.character(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...))
# Remove NAs and non-numeric values
data_no_missing <- na.omit(data[!is.na(data$exercise_duration) & data$exercise_duration > 0, ])
# Create a new factor variable for age groups
data_no_missing$age_group <- cut(data_no_missing$X1..Age, breaks = c(15, 25, 30, 35), labels = c("15-25", "26-30", "31-35"))
# Perform one-way ANOVA
anova_result <- aov(exercise_duration ~ age_group, data = data_no_missing)
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one age group has a different average exercise duration.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in average exercise duration across all age groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
# Identify and handle missing or non-numeric values in the exercise duration variable
data$exercise_duration <- as.numeric(as.character(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...))
# Check for NAs
na_indices <- which(is.na(data$exercise_duration))
if (length(na_indices) > 0) {
cat("Warning: NAs found in exercise duration. Rows with NAs will be excluded.\n")
data <- data[-na_indices, ]
}
# Check if there are still non-numeric values
non_numeric_indices <- which(!is.numeric(data$exercise_duration))
if (length(non_numeric_indices) > 0) {
cat("Warning: Non-numeric values found in exercise duration. Rows with non-numeric values will be excluded.\n")
data <- data[-non_numeric_indices, ]
}
# Create a new factor variable for age groups
data$age_group <- cut(data$X1..Age, breaks = c(15, 25, 30, 35), labels = c("15-25", "26-30", "31-35"))
# Perform one-way ANOVA
anova_result <- aov(exercise_duration ~ age_group, data = data)
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one age group has a different average exercise duration.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in average exercise duration across all age groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
#One-Way ANOVA
#H0:The average frequency of engaging in physical exercise per week is the same across all age groups (15-25, 26-30, 31-35).
#H1: At least one age group has a different average frequency of engaging in physical exercise per week
# Assuming your dataset is named 'data' and the relevant variable is 'X4..How.often.do.you.engage.in.physical.exercise.per.week.'
# Replace the specified values with the values you want to compare.
# Mapping the response options to numerical values
data$exercise_frequency <- factor(data$X4..How.often.do.you.engage.in.physical.exercise.per.week.,
levels = c("Never", "1 - 2 times", "3 - 4 times", "5 or more times"),
labels = c(0, 1, 3.5, 5))  # Assigning numerical values based on the midpoints
# Convert the factor variable to numeric
data$exercise_frequency <- as.numeric(as.character(data$exercise_frequency))
# Create a new factor variable for age groups
data$age_group <- cut(data$X1..Age, breaks = c(15, 25, 30, 35), labels = c("15-25", "26-30", "31-35"))
# Exclude missing values
data_no_missing <- na.omit(data)
length(data$age_group)
length(data$exercise_frequency)
# Perform one-way ANOVA
anova_result <- aov(exercise_frequency ~ age_group, data = data_no_missing)
length(anova_result)
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one age group has a different average frequency of engaging in physical exercise per week.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average frequency of engaging in physical exercise per week across all age groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
# Specify the null hypothesis value
specified_value <- 30  # Replace with your specified value
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "Employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "Employed", ]
# Check if there are enough observations for the t-test
if (nrow(employed_data) < 2) {
cat("Not enough data for employed individuals to perform the t-test.\n")
} else {
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
}
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Check if there are enough observations for the t-test
if (nrow(employed_data) < 2) {
cat("Not enough data for employed individuals to perform the t-test.\n")
} else {
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
}
# Specify the null hypothesis value
specified_value <- 30  # Replace with your specified value
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "Employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
# Specify the null hypothesis value
specified_value <- 30  # Replace with your specified value
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
# Specify the null hypothesis value
specified_value <- 30  # Replace with your specified value
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
# Specify the null hypothesis value
specified_value <- 30  # Replace with your specified value
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Ensure the variable is numeric and exclude missing values
exercise_duration_numeric <- as.numeric(as.character(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...))
exercise_duration_numeric <- exercise_duration_numeric[!is.na(exercise_duration_numeric)]
# Perform One-Sample t-Test
t_test_result <- t.test(exercise_duration_numeric, mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the specified value.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the specified value.\n")
}
# Calculate the overall mean of the variable
overall_mean <- mean(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., na.rm = TRUE)
# Specify the null hypothesis value as the overall mean
specified_value <- overall_mean
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the overall mean.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the overall mean.\n")
}
# Convert the variable to numeric
data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... <- as.numeric(as.character(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...))
# Calculate the overall mean of the variable
overall_mean <- mean(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., na.rm = TRUE)
# Specify the null hypothesis value as the overall mean
specified_value <- overall_mean
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from the overall mean.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from the overall mean.\n")
}
# Specify the null hypothesis value
specified_value <- 30
# Extract data for employed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
# Perform One-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session..., mu = specified_value)
# Display the results
cat("One-Sample t-Test Results for Employed Individuals:\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from 30 minutes.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session for employed individuals from 30 minutes.\n")
}
# Extract data for employed and unemployed individuals
employed_data <- data[data$X3..Occupation == "employed", ]
unemployed_data <- data[data$X3..Occupation == "unemployed", ]
# Perform Two-Sample t-Test
t_test_result <- t.test(employed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...,
unemployed_data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...)
# Display the results
cat("Two-Sample t-Test Results (Independent):\n")
cat("Test Statistic:", t_test_result$statistic, "\n")
cat("p-value:", t_test_result$p.value, "\n")
# Check if the p-value is less than the significance level (commonly 0.05)
if (t_test_result$p.value < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that the average time spent on each exercise session for employed individuals is different from unemployed individuals.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average time spent on each exercise session between employed and unemployed individuals.\n")
}
t_test_result
t_test_result$p.value
# Create a factor variable for occupation
data$occupation_group <- factor(data$X3..Occupation)
# Perform One-Way ANOVA
anova_result <- aov(X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... ~ occupation_group, data = data)
# Display the results
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one group has a different average time spent on each exercise session.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in average time spent on each exercise session across all groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
# Create boxplots to visualize the distribution of time spent on each exercise session
boxplot(X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... ~ occupation_group, data = data)
# Add labels and title
title("Boxplot of Time Spent on Each Exercise Session by Occupation Group")
xlabel <- "Occupation Group"
ylabel <- "Time Spent on Each Exercise Session"
axis(side = 1, at = 1:length(levels(data$occupation_group)), labels = levels(data$occupation_group))
axis(side = 2, label = ylabel)
# Create a two-way ANOVA model
anova_model <- aov(X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... ~ X3..Occupation * X2..Gender, data = data)
# Display the results
cat("Two-Way ANOVA Results:\n")
print(summary(anova_model))
# Check if there is a p-value for the interaction term
if (!is.null(summary(anova_model)$`Pr(>F)`[3])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_model)$`Pr(>F)`[3] < 0.05) {
cat("Reject the null hypothesis. There is evidence of interaction between employment status and gender, indicating a combined effect.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest an interaction between employment status and gender in influencing average exercise session time.\n")
}
} else {
cat("Unable to perform the two-way ANOVA test due to insufficient variability in the data.\n")
}
# Create a two-way ANOVA model
anova_model <- aov(X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... ~ X3..Occupation * X2..Gender, data = data)
# Display the results
cat("Two-Way ANOVA Results:\n")
print(summary(anova_model))
# Check if there is a p-value for the interaction term
if (!is.null(summary(anova_model)$`Pr(>F)`[3])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_model)$`Pr(>F)`[3] < 0.05) {
cat("Reject the null hypothesis. There is evidence of interaction between employment status and gender, indicating a combined effect.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest an interaction between employment status and gender in influencing average exercise session time.\n")
}
} else {
cat("Unable to perform the two-way ANOVA test due to insufficient variability in the data.\n")
}
# Create a two-way ANOVA model
anova_model <- aov(X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... ~ X3..Occupation + X2..Gender, data = data)
# Display the results
cat("Two-Way ANOVA Results:\n")
print(summary(anova_model))
# Check if there is a p-value for the interaction term
if (!is.null(summary(anova_model)$`Pr(>F)`[3])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_model)$`Pr(>F)`[3] < 0.05) {
cat("Reject the null hypothesis. There is evidence of interaction between employment status and gender, indicating a combined effect.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest an interaction between employment status and gender in influencing average exercise session time.\n")
}
} else {
cat("Unable to perform the two-way ANOVA test due to insufficient variability in the data.\n")
}
# Create a two-way ANOVA model
anova_model <- aov(X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... ~ X3..Occupation + X2..Gender, data = data)
# Display the results
cat("Two-Way ANOVA Results:\n")
print(summary(anova_model))
# Check if there is a p-value for the interaction term
if (!is.null(summary(anova_model)$`Pr(>F)`[3])) {
# Check if the p-value is less than the significance level (commonly 0.05)
if (summary(anova_model)$`Pr(>F)`[3] < 0.05) {
cat("Reject the null hypothesis. There is evidence of interaction between employment status and gender, indicating a combined effect.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest an interaction between employment status and gender in influencing average exercise session time.\n")
}
} else {
cat("Unable to perform the two-way ANOVA test due to insufficient variability in the data.\n")
}
#One-Way ANOVA
#H0:The average frequency of engaging in physical exercise per week is the same across all age groups (15-25, 26-30, 31-35).
#H1: At least one age group has a different average frequency of engaging in physical exercise per week
# Assuming your dataset is named 'data' and the relevant variable is 'X4..How.often.do.you.engage.in.physical.exercise.per.week.'
data$exercise_frequency <- factor(data$X4..How.often.do.you.engage.in.physical.exercise.per.week.,
levels = c("Never", "1 - 2 times", "3 - 4 times", "5 or more times"),
labels = c(0, 1, 3.5, 5))
data$exercise_frequency <- as.numeric(as.character(data$exercise_frequency))
data$age_group <- cut(data$X1..Age, breaks = c(15, 25, 30, 35), labels = c("15-25", "26-30", "31-35"))
data_no_missing <- na.omit(data)
length(data$age_group)
length(data$exercise_frequency)
anova_result <- aov(exercise_frequency ~ age_group, data = data_no_missing)
length(anova_result)
cat("One-Way ANOVA Results:\n")
print(summary(anova_result))
# Check if there is a p-value for the overall F-test
if (!is.null(summary(anova_result)$`Pr(>F)`[1])) {
if (summary(anova_result)$`Pr(>F)`[1] < 0.05) {
cat("Reject the null hypothesis. There is significant evidence that at least one age group has a different average frequency of engaging in physical exercise per week.\n")
} else {
cat("Fail to reject the null hypothesis. There is not enough evidence to suggest a significant difference in the average frequency of engaging in physical exercise per week across all age groups.\n")
}
} else {
cat("Unable to perform the ANOVA test due to insufficient variability in the data.\n")
}
# Assuming you have a data frame named 'df' with columns 'AgeGroup' and 'FrequencyPerWeek'
# 'AgeGroup' is a categorical variable (15-25, 26-30, 31-35), and 'FrequencyPerWeek' is the numeric variable of interest
# Fit Simple Linear Regression
model <- lm(FrequencyPerWeek ~ AgeGroup, data = df)
# Assuming you have a data frame named 'df' with columns 'AgeGroup' and 'FrequencyPerWeek'
# 'AgeGroup' is a categorical variable (15-25, 26-30, 31-35), and 'FrequencyPerWeek' is the numeric variable of interest
# Fit Simple Linear Regression
model <- lm(FrequencyPerWeek ~ AgeGroup, data = data)
# Assuming you have a data frame named 'df' with columns 'AgeGroup' and 'FrequencyPerWeek'
# 'AgeGroup' is a categorical variable (15-25, 26-30, 31-35), and 'FrequencyPerWeek' is the numeric variable of interest
# Fit Simple Linear Regression
model <- lm(data$FrequencyPerWeek ~ data$AgeGroup, data = data)
# Fit Simple Linear Regression
model <- lm(exercise_frequency ~ age_group, data = data)
# Print the summary of the regression model
summary(model)
plot(model)
# Print the summary of the regression model
summary(model)
# Remove missing values
df <- df[complete.cases(df$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...), ]
# Remove missing values
df <- data[complete.cases(df$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...), ]
data <- read.csv("Originaldata.csv")
df <- data[complete.cases(data$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...), ]
df$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session... <- as.numeric(df$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...)
# Add age and occupation to the data frame
df$age <- df$X1..Age
df$occupation <- df$X3..Occupation
# Define dependent variable (Y)
Y <- df$X5..On.average..how.many.minutes.do.you.spend.on.each.exercise.session...
# Define independent variable(s) (X)
X <- df$occupation
# If age is also included as an independent variable
X <- cbind(df$occupation, df$age)
# Perform Simple Linear Regression
model <- lm(Y ~ occupation + age, data = df)
# Print the summary of the regression model
summary(model)
plot(model)
# Perform Simple Linear Regression
model <- lm(Y ~ occupation + age, data = df)
# Print the summary of the regression model
summary(model)
plot(model)
